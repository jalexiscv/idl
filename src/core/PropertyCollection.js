/**
 * @fileoverview Define la clase PropertyCollection que representa una colección
 * de definiciones de propiedades. Esta clase vacía sirve como marcador de tipo
 * para colecciones de metadatos de propiedades en el sistema de binding de IDL.
 */

/** Clase que representa una colección de propiedades y sus metadatos asociados. */
class PropertyCollection {}
